{% set p = cms.contact %}

<section class="upx-gallery">
  <div class="upx-gallery__inner">
    <div class="upx-gallery__header">
      <h2 class="upx-gallery__heading">{{ p.heading }}</h2>
      <p class="upx-gallery__sub">{{ p.subtitle }}</p>
    </div>
  </div>
</section>

{% include "partials/lightbox.njk" %}
